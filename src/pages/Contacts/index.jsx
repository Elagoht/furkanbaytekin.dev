import Section from "../../components/Section";

export default function Contacts() {
  return <Section id="contacts">
    <div className="container">
      <fiv className="text-6xl font-title">
        Contacts.
      </fiv>
    </div>
  </Section>
}
